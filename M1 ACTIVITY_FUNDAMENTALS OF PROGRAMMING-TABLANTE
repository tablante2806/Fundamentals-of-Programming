

1. Create a program that counts to 100 by tens:
    #include <stdio.h>

int main() {
    int i;
    for(i = 10; i <= 100; i += 10) {
        printf("%d\n", i);
    }
    return 0;
}

2. Create a program that only print even numbers     between 0 and 10 (inclusive):

#include <stdio.h>

int main() {
    int i;
    
    for(i = 0; i <= 10; i++) {
        if(i % 2 == 0) {
            printf("%d\n", i);
        }
    }
    
    return 0;
}

3. Only print odd numbers:

#include <stdio.h>

int main() {
    int i;
    for(i = 0; i <= 10; i++) {
        if(i % 2 != 0) {
            printf("%d\n", i);
        }
    }
    return 0;
}

4. Print the powers of 2 up to 512.

#include <stdio.h>

int main() {
    int i, result = 1;
    for(i = 0; result <= 512; i++) {
        printf("8^%d = %d\n", i, result);
        result *= 8;
    }
    return 0;
}

5. Create a program that prints the multiplication table for a specified number.

#include <stdio.h>

int main() {
    int num, i;
    printf("Enter the Number: ");
    scanf("%d", &num);

    for(i = 1; i <= 10; i++) {
        printf("%d x %d = %d\n", num, i, num * i);
    }
    return 0;
}
